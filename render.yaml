services:
  # Server Service (Node.js)
  - type: web
    name: planning-dashboard-server
    env: node
    root: server
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18.16.0
    autoDeploy: false
